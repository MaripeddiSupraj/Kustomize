apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  labels:
    app: web-app
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: web-app
